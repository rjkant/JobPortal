[build]
dockerfile = "Dockerfile"

[deploy]
# Give the server plenty of time to run prisma push + boot Next.js
healthcheckPath = "/api/health"
healthcheckTimeout = 120

restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
